<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Habit Tracker</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f5f7fa;
      margin: 0;
      padding: 0;
      text-align: center;
    }

    .container {
      max-width: 600px;
      margin: 120px auto;
      padding: 20px;
      background: #ffffff;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h1 {
      font-size: 2.4em;
      margin-bottom: 10px;
      color: #333;
    }

    p {
      font-size: 1.2em;
      color: #555;
    }

    .btn {
      margin-top: 30px;
      padding: 12px 24px;
      background-color: #4CAF50;
      color: white;
      text-decoration: none;
      font-weight: bold;
      border-radius: 6px;
      transition: background-color 0.2s ease-in-out;
    }

    .btn:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Welcome to the Habit Tracker App</h1>
    <p>Start building better habits today.</p>

    {{#if session.user}}
      <a class="btn" href="/habits">Go to Dashboard</a>
    {{else}}
      <a class="btn" href="/login">Login to Get Started</a>
    {{/if}}
  </div>
</body>
</html>
