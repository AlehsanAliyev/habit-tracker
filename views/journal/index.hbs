<h2>📝 My Journal</h2>

<div class="bottom-actions">
  <a href="/journal/add" class="btn btn-add">➕ Add New Entry</a>
</div>

{{#each entries}}
  <div class="journal-entry {{#if @index}}{{#if (mod @index 2)}}right{{else}}left{{/if}}{{else}}left{{/if}}">
    <p><strong>{{date}}</strong></p>
    <p><em>🌅 Morning:</em> {{morningNote}}</p>
    <p><em>🌇 Evening:</em> {{eveningNote}}</p>
    <div class="habit-actions">
      <a href="/journal/edit/{{_id}}" class="btn btn-edit">✏️ Edit</a>
      <a href="/journal/delete/{{_id}}" class="btn btn-delete" onclick="return confirm('Delete this journal entry?')">🗑️ Delete</a>
    </div>
  </div>
{{/each}}
